{
  "dependencies": {
    "jquery": "^3.2.1",
    "jquery.gridly": "https://github.com/ksylvest/jquery-gridly"
  },
  "engines": {
    "yarn": ">= 1.0.0"
  },
  "scripts": {
    "postinstall": "node -e \"try { require('fs').symlinkSync(require('path').resolve('node_modules/@bower_components'), 'website_checker/frontend/static/libs', 'junction') } catch (e) { }\""
  }
}
